/*
 * Self executing function to run at end of page!
 */
(function() {
	var stylesheets = document.styleSheets;

	// Run through each CSS element.
		// Look for .onclick, .onmousedown
		// assign handlers to toggle classes.

	// Run through DOM
		//
})();